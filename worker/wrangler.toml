name = "mayo-ai-proxy"
main = "src/index.js"
compatibility_date = "2024-01-01"

# WICHTIG: Der GROQ_API_KEY gehoert NICHT in diese Datei (die landet im
# oeffentlichen Repo)! Er wird als Secret gesetzt - entweder ueber
# `wrangler secret put GROQ_API_KEY` oder im Cloudflare-Dashboard unter
# Worker -> Settings -> Variables and Secrets. Siehe README.md.
